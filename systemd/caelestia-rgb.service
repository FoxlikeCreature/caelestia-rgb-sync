[Unit]
Description=caelestia RGB daemon — sync lighting to theme accent color
After=openrgb.service
BindsTo=openrgb.service

[Service]
Type=simple
ExecStart=/usr/bin/caelestia-rgb-sync daemon
Restart=on-failure
RestartSec=5

[Install]
WantedBy=graphical-session.target
